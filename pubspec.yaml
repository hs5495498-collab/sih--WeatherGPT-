name: weathergpt
description: "WeatherGPT — Conversational AI weather assistant (SIH26068)"
publish_to: 'none'
version: 2.0.0+3

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.0.5
  geolocator: ^13.0.1
  url_launcher: ^6.3.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  speech_to_text: ^7.0.0
  flutter_tts: ^4.1.0
  permission_handler: ^11.3.1
  supabase_flutter: ^2.8.0
  package_info_plus: ^8.1.1
  tflite_flutter: ^0.11.0
  image_picker: ^1.1.2
  image: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    
